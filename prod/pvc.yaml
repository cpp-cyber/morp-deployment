apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: morp-pvc
  namespace: morp
spec:
  storageClassName: local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
