apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: morp-pvc
  namespace: morp
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
