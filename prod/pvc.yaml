apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: morp-pvc
  namespace: morp
spec:
  storageClassName: morp-local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
